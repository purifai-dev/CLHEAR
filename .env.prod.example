# ── Database ──────────────────────────
POSTGRES_DB=clhear
POSTGRES_USER=clhear_user
POSTGRES_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD

# ── App ───────────────────────────────
APP_BASE_URL=https://clhear.ai

# ── Email (optional — leave blank to use demo mode) ──
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=alerts@clhear.ai
