version: "3.8"
services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: clhear
      POSTGRES_USER: clhear_user
      POSTGRES_PASSWORD: dev123
volumes:
  postgres_data:
